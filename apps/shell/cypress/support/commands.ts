/// <reference types="cypress" />

// No custom commands needed for this test suite.
// The app uses demo authentication (AuthService starts authenticated)
// so cy.visit('/dashboard') works directly.
